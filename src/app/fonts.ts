import { Gideon_Roman, Merriweather } from "next/font/google";

export const gideonRoman = Gideon_Roman({
  weight: "400",
  variable: "--font-gideon-roman",
  subsets: ["latin"],
});

export const merriweather = Merriweather({
  weight: "300",
  variable: "--font-merriweather",
  subsets: ["latin"],
});
